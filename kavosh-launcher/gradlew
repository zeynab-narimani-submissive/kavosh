#!/bin/sh
# Standard Gradle wrapper launcher.
# If this file lacks the gradle-wrapper.jar (not included, no network in build env),
# open the project in Android Studio and it will regenerate the wrapper automatically,
# or run: gradle wrapper --gradle-version 8.9
DIR="$(cd "$(dirname "$0")" && pwd)"
exec gradle "$@"
